package com.novaraspace.model.enums.audit;

public enum AuditScheduledTaskType {
    FLIGHT_CLEANUP,
    FLIGHT_GENERATION,
    REFRESH_TOKEN_CLEANUP,
    BOOKING_CLEANUP,
    BOOKING_QUOTE_CLEANUP,
}
